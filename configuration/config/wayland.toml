environment.systemPackages = [
    # Dmenu-like launcher/menu
    "fuzzel",
    # Default terminal
    "alacritty",
    # X11 compatibility
    "xwayland-satellite",
]

[programs.niri]
enable = true

[programs.waybar]
enable = true
